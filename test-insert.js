import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.from('assignment_logs').insert([{ item_type: 'Terminal', item_id: 1, action: 'Préstamo', employee: 'Test', notes: null, item_detail: '{}' }]).select().single(); console.log('Data:', data); console.log('Error:', error); } test();
